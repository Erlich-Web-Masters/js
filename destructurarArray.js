const persona = ['Erlich Airam',7,'28 de julio de 2013',4.20];

const[nombre,edad,fecha,hora] = persona;

let oracion = `Mi hijo ${nombre} tiene ${edad} su cumple es ${fecha} y nacio a las ${hora}am`;
console.log(oracion);
